import { logger } from '../../core/logger.js';
export default {
        name: 'embedTemplates',
            init() { 
                        logger.info('Embed Templates module restored and initialized'); 
            }
};
